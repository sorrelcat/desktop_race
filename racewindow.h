#ifndef RACEWINDOW_H
#define RACEWINDOW_H

#include <QWidget>
#include <QFrame>

class RaceWindow : public QWidget
{
    Q_OBJECT
public:
    explicit RaceWindow(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // RACEWINDOW_H
